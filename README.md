# Docker on Vagrant

Docker on Vagrant to experiment with different versions/settings of Docker.

That's all folks !

This version creates a Vagrant machine for each Docker engine version between 1.9 and 1.12.
